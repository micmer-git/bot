# micmer bot dashboard

Dynamic ops dashboard (cron + sessions + kanban) published on GitHub Pages.

## Password gate
This build uses a lightweight client-side password screen for convenience.
For real protection, put it behind Cloudflare Access / reverse-proxy auth.
